{
  "name": "fit4110-lab05-docker-compose-readiness",
  "version": "0.1.0",
  "description": "FIT4110 Lab 05 - Docker Compose readiness and end-to-end verification for Smart Campus services",
  "private": true,
  "scripts": {
    "mock:iot": "prism mock contracts/access-gate.openapi.yaml --host 0.0.0.0 --port 4010",
    "lint:openapi": "spectral lint contracts/*.yaml",
    "test:compose": "mkdir -p reports && newman run postman/collections/FIT4110_lab05_local.postman_collection.json --environment postman/environments/_local.postman_environment.json --reporters cli,htmlextra,junit --reporter-htmlextra-export reports/newman-lab05-compose.html --reporter-junit-export reports/newman-lab05-compose.xml --reporter-htmlextra-title \"FIT4110 Lab05 - Smart Campus Gate API\" --color on"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^5.10.0",
    "@stoplight/spectral-cli": "^6.13.1",
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1",
    "wait-on": "^8.0.1"
  }
}